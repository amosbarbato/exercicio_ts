function multiply(num1: number, num2: number): number {
    const resulted = num1 * num2;
    console.log(`${num1} x ${num2} = ${resulted}`);
    return resulted;
}

function welcome(nome: string): string {
    const message = `Olá ${nome}, seja bem-vindo!`;
    console.log(message);
    return message;
}